import { style } from "@vanilla-extract/css";

export const header = style({});

export const tempFactionBox = style({
  display: "inline-block",
  width: "1rem",
  height: "1rem",
  borderRadius: "50%",
  position: "relative",
  top: "0.1rem",
});

export const table = style({
  // center all tds
  textAlign: "center",
});
